#include<stdio.h>
  
     void main(){
      char ch;
	    printf("Enter the character ch :");
     	scanf("%c",&ch);
     	
     (ch>='A' && ch<='Z' || ch>='a' && ch<='z')? printf("the number is an alphabet"):printf("the number is not an alphabet");	
     	
		 	
	}
