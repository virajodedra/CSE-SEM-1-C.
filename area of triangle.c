#include<stdio.h>
  int  main() {
  	int h,l,area;
  	printf("fill the value of h :");
  	scanf("%d",&h);
  	printf("fill the value of l :");
  	scanf("%d",&l);
  	 
  	 area=(h*l)/2;
  	 printf("the area is :%d",area);
  }
