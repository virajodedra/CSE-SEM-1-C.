#include<stdio.h>
   
      void main()    { 
      float b,h,area;
      printf("enter the height of triangle :");
      scanf("%f",&h);
      printf("enter the base of triangle :");
      scanf("%f",&b);
      area=(b*h)/2;
      printf("the area is: %f",area);
      }
